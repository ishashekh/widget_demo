<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/Widget.Widget_demo.AppWidget.Container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0081D4FA"
    android:theme="@style/Theme.Widget_demo.AppWidgetContainer">

    <StackView
        android:id="@+id/widget_stack"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:loopViews="true"/>
    <!--TextView
        android:id="@+id/appwidget_text"
        style="@style/Widget.Widget_demo.AppWidget.InnerView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:layout_margin="8dp"
        android:contentDescription="@string/appwidget_text"
        android:text="@string/appwidget_text"
        android:textSize="24sp"
        android:textStyle="bold|italic"
        android:background="@color/purple_500"
        android:textColor="@color/white"/-->
</FrameLayout>